# 轻量级跨平台SSH远程代码编辑器\-完整开发需求文档（V1\.0）

## 0\. 项目核心定位与目标

### 0\.1 项目定义

开发一款**跨平台、轻量级桌面GUI SSH远程代码编辑器**，用于完全替代 VSCode Remote\-SSH，专门适配嵌入式弱性能ARM Linux板卡开发场景。

### 0\.2 核心设计理念

**本地重计算、远程零负载**

- 所有GUI渲染、代码编辑、语法高亮、Git Diff解析、文件缓存均在本地PC完成

- 远程嵌入式板卡仅保留系统原生SSH、SFTP、Git基础能力，**禁止运行任何本项目常驻服务、代理、语言服务**

### 0\.3 核心架构链路

本地PC（GUI编辑器/语法解析/Git处理/缓存/SSH客户端） ← SSH/SFTP协议 → 远程ARM板卡（原生sshd、sftp、Git仓库、源码文件）

### 0\.4 版本定位（V1\.0 非IDE）

本版本仅聚焦**远程代码浏览、编辑、保存、语法高亮、Git行级差异标记**，不做全功能IDE，严控开发复杂度。

## 1\. 硬性强制需求（不可修改）

### 1\.1 客户端形态：纯桌面GUI应用

必须为标准 Desktop GUI 桌面程序，满足：

- 支持全鼠标可视化操作

- 非Web IDE、非浏览器应用、非VSCode插件、非纯终端程序、非Vim/Neovim工具

### 1\.2 跨平台兼容要求

必须同时适配两大主流平台，支持免环境安装运行：

- Windows：适配 Win10/Win11，打包为 `RemoteCodeEditor.exe` 可执行文件

- Ubuntu：适配 22\.04\+，优先打包为 `RemoteCodeEditor.AppImage`，可选\.deb安装包

用户运行要求：无需安装Python、无需配置依赖，双击即可启动。

### 1\.3 远端零负载硬性约束

远程ARM板卡**禁止运行**任何重型常驻服务：

- 禁止：VSCode Server、Remote Agent、LSP语言服务、索引服务、Node\.js常驻服务、本项目自定义后台服务

- 远端仅需系统原生能力：sshd、sftp、Git、标准Linux文件系统

### 1\.4 V1\.0 功能边界（明确非目标）

第一版本**不实现**以下功能，严控范围：

- LSP代码补全、智能提示、语义分析

- 代码调试、编译、运行系统

- Docker集成、终端集成、远程项目索引

- 插件系统、AI助手、全量IDE工作区能力

## 2\. 典型使用场景

1\. 开发者使用Windows/Ubuntu本地电脑，启动桌面客户端

2\. 选择/新增ARM板卡SSH连接（RK3566/RK3588/香橙派等嵌入式Linux设备）

3\. 连接成功后，可视化浏览远程板卡代码目录

4\. 打开源码文件在线编辑，本地实时渲染

5\. Ctrl\+S保存，通过SFTP上传覆盖远程文件

6\. 编辑器左侧行号区域，实时彩色标记当前文件相对于Git仓库HEAD的改动（新增/修改/删除）

## 3\. 整体系统架构

采用**前后端解耦、模块化分层架构**，GUI与网络、Git、文件逻辑完全隔离

### 3\.1 本地客户端分层

- UI层：主窗口、主机管理、文件树、编辑器、状态栏、设置界面

- 核心能力层：SSH/SFTP客户端、Git解析、文件缓存、编码处理、异步任务

- 工具层：日志系统、配置管理、异常捕获、跨平台适配

### 3\.2 远端设备分层

仅保留系统原生基础服务，无任何自定义常驻进程，仅临时执行Git查询、文件传输指令。

## 4\. 技术选型规范

### 4\.1 核心技术栈（优先）

Python \+ PySide6/Qt

### 4\.2 选型理由

- 完美支持Windows/Linux跨平台GUI开发

- 组件成熟（文件树、多标签、Dock窗口、右键菜单）

- SSH/SFTP、文件处理、Git解析生态完善

- 支持PyInstaller一键打包独立可执行程序

### 4\.3 选型优先级

稳定性 \> 开发效率 \> 跨平台兼容性 \> 性能 \> 扩展性 \> 技术新颖度

备注：若当前组件存在致命问题，可替换合理跨平台方案，禁止为炫技增加复杂度。

## 5\. 核心功能详细需求

### 5\.1 SSH远程连接能力

#### 5\.1\.1 连接参数

支持Host/IP、端口、用户名、密码、SSH私钥、私钥密码验证两种认证方式（密码/私钥）

#### 5\.1\.2 核心能力

- 多主机保存管理、快速连接、断线自动重连

- 连接超时、认证失败、网络异常等清晰文字报错

- 不依赖系统OpenSSH命令行，纯代码封装SSH/SFTP逻辑

- 模块独立封装，与UI解耦

### 5\.2 SSH主机管理

可视化主机管理面板，支持新增、编辑、删除、快速连接设备

单主机配置项：设备名称、IP/域名、端口、用户名、认证方式、私钥路径、远程工作目录

安全规范：密码禁止明文存储，第一版支持临时输入密码，后续可对接系统安全存储

### 5\.3 远程文件浏览

可视化远程文件树，支持目录展开/收起、文件预览打开

右键菜单功能：新建文件/文件夹、删除、重命名、刷新目录、查看基础文件信息

### 5\.4 文件编辑与同步机制（核心）

采用**本地缓存编辑、保存后上传**机制，杜绝实时高频SSH请求：

1. 打开文件：SFTP下载远程文件至本地内存/临时缓存

2. 编辑过程：纯本地操作，无网络请求，零延迟

3. 保存文件：Ctrl\+S触发SFTP上传，覆盖远程文件

优势：适配弱网络、避免编辑卡顿、降低板卡与网络开销

### 5\.5 基础编辑器能力

必备基础功能：行号显示、当前行高亮、自动缩进、Tab/空格适配、撤销/重做、全量快捷键、复制粘贴、搜索替换、多标签页、未保存状态标记、鼠标精准选中滚动

### 5\.6 语法高亮

V1\.0必支持：C、C\+\+、Python、Bash、YAML、JSON、XML、Markdown、CMake

架构要求：高亮规则可扩展，后续可新增文件类型，无需重构核心逻辑

约束：仅做语法高亮，不实现任何LSP语义解析

### 5\.7 Git行级差异标记（核心核心功能）

#### 5\.7\.1 标记规则

编辑器左侧Gutter行号区域彩色标记，对标VSCode效果：

- 新增行：绿色标记

- 修改行：黄色/橙色标记

- 删除行：红色标记

颜色配置集中管理，可自定义主题，禁止硬编码

#### 5\.7\.2 Diff对比基准

默认对比：当前远程文件工作区 \& Git HEAD基准版本

#### 5\.7\.3 特殊场景适配

文件删除行识别：已删除的不存在行，仍在对应行号位置显示删除标记，提示用户变更

#### 5\.7\.4 Diff获取逻辑

- 通过SSH远程执行轻量化Git原生命令（git diff、git status等）

- Diff文本传回本地，由本地程序解析渲染标记

- 禁止远端搭建Git服务、禁止全量拉取仓库、禁止常驻Git进程

#### 5\.7\.5 刷新策略

- 打开文件时：一次性获取Diff数据渲染初始标记

- 编辑过程：仅本地标记脏状态，不频繁请求远端Git接口

- 保存文件后：按需延迟刷新Diff

- 提供手动刷新Git按钮，保证稳定性优先

### 5\.8 Git仓库识别

打开远程目录自动判断是否为Git仓库：

- 非仓库：状态栏提示「Git: Not a repository」

- 正常仓库：显示当前分支、文件变更状态

### 5\.9 本地缓存机制

可缓存：SSH主机配置、最近打开文件、编辑器状态、Git Diff临时数据

核心约束：远程文件为唯一真实数据源，缓存仅做体验优化，不替代远端数据

### 5\.10 文件冲突处理（重要）

场景：本地编辑未保存，远程文件被外部修改

触发保存时弹窗提示，提供三选项：重新加载远程文件、覆盖远程文件、取消保存

禁止静默覆盖远端文件，避免代码丢失

### 5\.11 文件编码兼容

默认支持UTF\-8、UTF\-8 BOM、ASCII编码

未知编码时弹窗提示用户手动选择，避免程序崩溃

### 5\.12 大文件与UI性能优化

支持MB级文件正常加载编辑，文件下载、解析、渲染全部异步处理，禁止阻塞GUI主线程，杜绝窗口卡死、无响应问题

### 5\.13 异步线程规范

以下操作必须异步子线程执行，不阻塞UI：SSH连接、SFTP上传下载、远端Git命令查询、目录列表获取、文件解析

### 5\.14 异常容错

全面适配超时、连接拒绝、断连、权限不足、文件不存在等异常场景，弹窗友好提示，程序不崩溃、不闪退

### 5\.15 状态栏与设置

状态栏展示：连接状态、Git分支、文件类型、编码、保存状态

基础设置：主题、字体大小、缩进尺寸、自动保存、SSH基础配置、编辑器配置

### 5\.16 日志与安全规范

- 本地日志目录记录连接、传输、Git操作、异常报错

- 严格脱敏：日志禁止打印、记录密码、私钥明文内容

## 6\. 跨平台打包与交付

### 6\.1 Windows交付

PyInstaller打包独立exe程序，干净环境双击直接运行，无依赖、无需Python环境

### 6\.2 Ubuntu交付

优先打包AppImage可执行文件，授权后直接运行，适配Ubuntu22\.04\+；可选deb安装包

### 6\.3 跨平台代码规范

- 核心业务代码全平台通用，仅系统特殊逻辑差异化处理

- 使用pathlib处理路径，禁止硬编码Windows/Linux绝对路径

- 配置文件、缓存目录、密钥路径做跨平台适配

## 7\. 项目目录结构规范

模块化分层，杜绝巨型文件、逻辑堆砌，UI与核心能力完全解耦

```Plain Text
remote-code-editor/
├── app/                    # 核心业务代码
│   ├── main.py             # 程序入口
│   ├── ui/                 # 所有界面组件
│   │   ├── main_window.py
│   │   ├── ssh_dialog.py
│   │   ├── host_manager.py
│   │   └── widgets/
│   ├── editor/             # 编辑器核心、高亮、Git标记
│   ├── remote/             # SSH/SFTP/远程文件系统
│   ├── git/                # Git解析、Diff处理
│   ├── config/             # 配置管理
│   ├── cache/              # 本地缓存逻辑
│   └── utils/              # 通用工具、日志、异常
├── tests/                  # 单元测试用例
├── scripts/                # 跨平台打包脚本
│   ├── build_windows.bat
│   └── build_linux.sh
├── requirements.txt
├── pyproject.toml
├── README.md
└── LICENSE
```

## 8\. 代码质量要求

- 全代码类型提示、命名规范、模块化拆分

- 完善异常捕获处理，无未捕获报错

- 核心能力独立封装（SSH、Git、编辑器、缓存）

- 禁止所有逻辑堆砌在主窗口文件

- 覆盖基础单元测试，重点全覆盖Git Diff各类场景

## 9\. 测试用例要求（重点Git Diff）

必须覆盖12类核心Diff场景：纯新增、纯修改、纯删除、新增\+修改、修改\+删除、全混合变更、文件头尾变更、连续多行变更、空文件、新文件、无变更文件等，保证标记精准无误

同时全覆盖SSH连接、SFTP传输、编辑器操作、异常容错场景测试

## 10\. 分阶段开发顺序（严格执行）

Phase1：搭建项目结构 \+ 基础GUI框架（主窗口、文件树、连接弹窗）

Phase2：实现SSH/SFTP核心能力（连接、目录浏览、文件打开/保存）

Phase3：完善编辑器能力（语法高亮、多标签、搜索替换）

Phase4：核心功能落地（Git解析、Diff渲染、行级彩色标记）

Phase5：稳定性优化（异步线程、断连重连、冲突处理、编码兼容、容错优化）

Phase6：跨平台打包、全场景实测、验收交付

## 11\. 验收标准（全部必须满足）

1. **远端零负载**：全新ARM板卡仅开启原生SSH服务，即可连接使用，无任何第三方常驻进程

2. **Windows适配**：干净环境双击exe直接启动，功能正常

3. **Ubuntu适配**：22\.04\+可正常运行AppImage程序

4. **远程编辑可用**：正常连接板卡、浏览目录、编辑代码、保存同步远端文件

5. **Git标记精准**：新增/修改/删除行彩色标记识别准确，覆盖全场景

6. **弱设备适配**：远端无VSCode系列重型进程，仅临时Git/SFTP短时进程

7. **UI不卡顿**：网络请求、文件加载全程UI不冻结、可正常操作

## 12\. 核心产品原则（最终准则）

远端低负载 \> 本地GUI流畅体验 \> 纯SSH/SFTP协议通信 \> 无远端代理服务 \> 不做冗余IDE功能 \> Git Diff精准可用 \> 双平台兼容 \> 鼠标可视化操作 \> 程序稳定性优先

> （注：部分内容可能由 AI 生成）
